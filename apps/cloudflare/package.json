{
  "name": "@sunup/cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply sunup --remote",
    "db:migrate:local": "wrangler d1 migrations apply sunup --local"
  },
  "dependencies": {
    "@sunup/core": "*",
    "slack-edge": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
